apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "as400-rpc.fullname" . }}-test-connection"
  labels:
    {{- include "as400-rpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "as400-rpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
